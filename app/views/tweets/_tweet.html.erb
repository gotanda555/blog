 <div class="teto">
  <p><%= tweet.text %></p>
  <% if user_signed_in? && current_user.id == tweet.user_id %>
  <div class="tete">
  <%= link_to '削除', "/tweets/#{tweet.id}", method: :delete , class:'woo' %>
  <%= link_to '編集', "/tweets/#{tweet.id}/edit", method: :get , class:'woo' %>
  </div>
  <% end %>
  </div>